#include <stdio.h>
/**
 * main - program that prints its name, followed by a new line
 * @argc: entier de type int
 * @argv: POinteur
 * Return: 0
 */
int main(int argc, char *argv[])
{
    printf("%s\n", argv[0]);
    return (0);
}
