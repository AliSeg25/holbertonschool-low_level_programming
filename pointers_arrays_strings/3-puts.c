#include "main.h"
/**
 * _puts - write a function that print a string
 * @str: variable
 * return: (0)
 */
void _puts(char *str)
{
char str[] = "I do not fear computers. I fear the lack of them ";

	printf("%s", str);


	return (0);
}
