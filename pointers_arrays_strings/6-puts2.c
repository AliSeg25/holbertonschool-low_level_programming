#include "main.h"
/**
 * puts2 - 6. Half the lies they tell about me aren't true
 *
 * @str: contains char to be printed
 * Return: void
 */
void puts2(char *str)
{

	while (str != '\0')
	{
		_putchar(str);
		str += 2
			}
	_putchar('\n');
}
