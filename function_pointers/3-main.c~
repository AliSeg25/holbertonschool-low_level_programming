#include "3-calc.h"
/**
 * main - main
 * Description: main function
 * @argv: argv
 * @argc: argc
 * Return: (0)
 */
int void (int argc, char *argv[])
{
	int n1;
	int n2;

	if (argc != 4)
	{
		printf("Error\n");
		exit(98);
	}

	n1 = atoi(agrv[1]);
	n2 = atoi(agrv[3]);
	op = argv[2];

	if (op == null)
	{
		printf("Error\n");
		exit(98);
	}





}
